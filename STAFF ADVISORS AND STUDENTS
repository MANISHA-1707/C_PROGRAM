#include<stdio.h>
#include<stdlib.h>
int main()
{
   int n,j=0,k=0,i;
   int *pl,*po,*pe,t,s;
   printf("Enter the number of students:");
   scanf("%d",&n);
   p1=(int*)malloc(n*sizeof(int));
   printf("Enter the first roll number:");
   scanf("%d",&s);
   t=s;
   for(i=0;i<n;i++)
   {
      pl[i]=t;
      t++;
   }
   if(n%2==0)
   {
      po=(int*)malloc((n/2)*sizeof(int));
      pe=(int*)malloc((n/2)*sizeof(int));
   }
   else
      {
          po=(int*)malloc(((n/2)+1)*sizeof(int));
          pe=(int*)malloc(((n/2)+1)*sizeof(int));
      }
      for(i=0;i<=n;i++)
      {
         if(s%2==0)
         {
            if(i%2==0)
            {
               pe[j]=pl[i];
               j++;
            }
            else
            {
               po[k]=pl[i];
               k++;
            }
         }
         else
         {
            if(i%2==0)
            {
               pe[j]=pl;
               j++;
            }
            else
            {
               po[k]=pl[i];
               k++;
            }
         }
      }
   printf("The students under staff advisors 1");
   for(i=0;i<j;i++)
   {
      printf("%d\n",po[i]);
   }
   printf("The students under staff advisors 2");
   for(i=0;i<k;i++)
   {
      printf("%d\n",pe[i]);
   }
}
